<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>แก้ไขข้อมูลประเพณี</title>
    <link rel="stylesheet" type="text/css" href="/css/edit.css">
</head>
<body>
    <div class="container">
        <h1>แก้ไขข้อมูลประเพณี</h1>
        <form action="/tradition/update/<%= tradition.id %>" method="POST" enctype="multipart/form-data" class="edit-form">
            <div class="form-group">
                <label for="name">ชื่อประเพณี:</label>
                <input type="text" id="name" name="name" value="<%= tradition.name %>" required>
            </div>
            <div class="form-group">
                <label for="history">ประวัติ:</label>
                <textarea id="history" name="history" rows="1" required oninput="autoExpand(this)"><%= tradition.history %></textarea>
            </div>
            <div class="form-group">
                <label for="description">รายละเอียด:</label>
                <textarea id="description" name="description" rows="1" required oninput="autoExpand(this)"><%= tradition.description %></textarea>
            </div>
            <div class="form-group">
                <label for="date">วันที่:</label>
                <input type="text" id="date" name="date" value="<%= tradition.date %>" required>
            </div>
            <div class="form-group">
                <label for="image">รูปภาพ:</label>
                <input type="file" id="image" name="image">
            </div>
            <div class="form-actions">
                <button type="submit" class="submit-btn">อัปเดตข้อมูล</button>
                <button type="button" onclick="window.location.href='/edit-info'" class="cancel-btn">ยกเลิก</button>
            </div>
        </form>
    </div>

    <script>
        function autoExpand(field) {
            field.style.height = 'auto';
            field.style.height = field.scrollHeight + 'px';
        }

        // Initialize the textarea height on page load
        document.querySelectorAll('textarea').forEach(textarea => autoExpand(textarea));
    </script>
</body>
</html>
